name: chat_gpt_flutter_custom_json
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.3.3 <4.0.0'
dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  # chat_gpt_sdk: ^2.1.2
  # chat_gpt_flutter: ^1.0.16
  http: ^1.2.1

flutter:
  uses-material-design: true
  assets:
    - assets/